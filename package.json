{
  "name": "manuelaklenke-cypress-e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "cypress run --browser chrome",
    "test:headed": "cypress open",
    "test:report": "npx mochawesome-merge cypress/reports/*.json -o cypress/reports/merged.json && npx marge cypress/reports/merged.json --reportDir mochawesome-report --inline"
  },
  "devDependencies": {
    "cypress": "^13.0.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0",
    "typescript": "^5.0.0"
  }
}
